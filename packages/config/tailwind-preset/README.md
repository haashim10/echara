# Echara Tailwind Preset (placeholder)

This package will host the shared Tailwind CSS preset once additional design tokens and utilities are extracted from the product surface. For now, the apps consume the shared design tokens from `@echara/ui`.
